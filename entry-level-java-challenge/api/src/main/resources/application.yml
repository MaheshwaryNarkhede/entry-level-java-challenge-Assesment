server:
  port: 8080

spring:
  application:
    name: EntryLevelJavaChallengeApplication

logging:
  level:
    org.springframework: INFO

management:
  endpoints:
    web:
      exposure:
        include: "*"